[phases.setup]
# Ensure a stable Node runtime on Railway/Nixpacks
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["cd server", "npm ci"]

[phases.build]
cmds = []

[start]
cmd = "cd server && npm start"
